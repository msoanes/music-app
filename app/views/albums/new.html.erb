<h1>New Album</h1>
<%= render 'shared/errors' %>
<%= render 'form',
           action: albums_url,
           method: 'post',
           submit_text: 'Submit album',
           album: @album,
           bands: @bands %>
